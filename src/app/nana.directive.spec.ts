import { NanaDirective } from './nana.directive';

describe('NanaDirective', () => {
  it('should create an instance', () => {
    const directive = new NanaDirective();
    expect(directive).toBeTruthy();
  });
});
